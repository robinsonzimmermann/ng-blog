import { SwitchMediaDirective } from './switch-media.directive';

describe('SwitchMediaDirective', () => {
  it('should create an instance', () => {
    const directive = new SwitchMediaDirective();
    expect(directive).toBeTruthy();
  });
});
